<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rbac.dao.MSysPermissionDao">

    <select id="findByRoleId" parameterType="java.lang.Integer" resultType="com.example.rbac.entity.MSysPermission">
        select
            msp.ID as id, msp.PERMISSION_NAME as permissionName, msp.PERMISSION_NAME_ZH as permissionNameZh
        from
            M_SYS_ROLE_PERMISSION msrp left join M_SYS_PERMISSION msp on msrp.PID = msp.ID
        where
            RID = #{id,jdbcType=INTEGER}
    </select>

</mapper>