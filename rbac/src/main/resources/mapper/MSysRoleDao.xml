<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rbac.dao.MSysRoleDao">
    <select id="findByOperatorId" parameterType="java.lang.Integer" resultType="com.example.rbac.entity.MSysRole">
        select
            msr.ID as id, msr.ROLE_NAME as roleName, msr.ROLE_NAME_ZH as roleNameZh
        from
            M_SYS_OPERATOR_ROLE msor left join M_SYS_ROLE msr on msor.RID = msr.ID
        where
            UID = #{id,jdbcType=INTEGER}
    </select>

</mapper>